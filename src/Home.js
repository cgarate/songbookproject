import React from 'react';
import { Container, Heading } from 'native-base';

const Home = ({ navigation }) => {
  return (
    <Container flex={1} bg="white" w="100%">
      <Heading>Songbook</Heading>
    </Container>
  );
};

export default Home;
